Определите математическую модель
Выберите тип возвращаемого значения
Объявите функцию с параметром
Реализуйте базовый случай (условие остановки)
Добавьте рекурсивный вызов
Скомбинируйте результат
Проверьте граничные условия

Код:

#include <iostream>
#include <stdexcept>

// Шаг 2–6: реализация рекурсивной функции
unsigned long long factorial(int n) {
    // Шаг 7: проверка граничных условий
    if (n < 0) {
        throw std::invalid_argument("Factorial of negative number is undefined");
    }
    
    // Шаг 4: базовый случай
    if (n <= 1) {
        return 1;
    }
    
    // Шаг 5–6: рекурсивный вызов и композиция результата
    return n * factorial(n - 1);
}

int main() {
    try {
        int number = 5;
        std::cout << number << "! = " << factorial(number) << std::endl;
    } catch (const std::exception& e) {
        std::cerr << "Error: " << e.what() << std::endl;
        return 1;
    }
    return 0;
}
